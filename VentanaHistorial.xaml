<Window x:Class="FocusPomodoro.VentanaHistorial"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de Concentración" 
        Height="500" 
        Width="600"
        MinHeight="350"
        MinWidth="400"
        Topmost="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#aaa"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Width="28" Height="28">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#ff4757"/>
                    <Setter Property="Background" Value="#ffffff15"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#ff475790"/>
            <Setter Property="Foreground" Value="#fff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ff6b7aa0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" 
            Background="#1a1a2ec0" 
            BorderBrush="#ffffff15" 
            BorderThickness="1"
            x:Name="MainBorder">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Historial de Concentracion" 
                           FontSize="18" FontWeight="Bold" 
                           Foreground="#00d9ff" VerticalAlignment="Center"/>
                <Button x:Name="CloseBtn" Content="×" 
                        Width="30" Height="30" 
                        HorizontalAlignment="Right"
                        Click="CloseBtn_Click"
                        Style="{StaticResource TitleButtonStyle}"/>
            </Grid>

            <Border Grid.Row="1" Background="#2d2d4480" CornerRadius="10" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Sesiones Hoy" FontSize="10" Foreground="#888"/>
                        <TextBlock x:Name="TodaySessionsText" Text="0" 
                                   FontSize="20" FontWeight="Bold" Foreground="#00d9ff"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Eficiencia" FontSize="10" Foreground="#888"/>
                        <TextBlock x:Name="TodayEfficiencyText" Text="0%" 
                                   FontSize="20" FontWeight="Bold" Foreground="#fbbf24"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ListView Grid.Row="2" x:Name="SessionsListView" 
                      Background="Transparent" 
                      BorderThickness="0"
                      Foreground="#fff"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="8" Padding="10" Margin="0,0,0,5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#2d2d4480"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInProgress}" Value="True">
                                            <Setter Property="Background" Value="#3d5a8080"/>
                                            <Setter Property="BorderBrush" Value="#00d9ff"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding StartTime, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" 
                                                       FontSize="12" Foreground="#888"/>
                                            <TextBlock Text=" (En progreso)" FontSize="12" Foreground="#00d9ff" FontWeight="Bold"
                                                       Visibility="{Binding IsInProgress, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="Duracion: " FontSize="11" Foreground="#888"/>
                                            <TextBlock Text="{Binding TotalDurationMinutes}" FontSize="11" Foreground="#fff"/>
                                            <TextBlock Text=" min" FontSize="11" Foreground="#888"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Efficiency, StringFormat='{}{0}%'}" 
                                                   FontSize="16" FontWeight="Bold"
                                                   Foreground="{Binding EfficiencyColor}"/>
                                    </StackPanel>
                                </Grid>
                                
                                <StackPanel Margin="0,8,0,0" Visibility="{Binding HasSegments, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="Progreso de concentracion:" FontSize="10" Foreground="#00d9ff" FontWeight="Bold"/>
                                    <ItemsControl ItemsSource="{Binding SegmentosEnfoque}" Margin="5,3,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <TextBlock FontSize="10" Foreground="#aaa">
                                                        <Run Text="- "/>
                                                        <Run Text="{Binding FormattedDuration, Mode=OneWay}" Foreground="#4ade80"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Ultimos 7 dias: " FontSize="11" Foreground="#888" VerticalAlignment="Center"/>
                    <TextBlock x:Name="WeekSummaryText" Text="" FontSize="11" Foreground="#00d9ff" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="1" x:Name="ClearHistoryBtn" 
                        Content="Limpiar Historial" 
                        Click="ClearHistoryBtn_Click"
                        Style="{StaticResource ClearButtonStyle}"/>
            </Grid>
        </Grid>
    </Border>
</Window>
